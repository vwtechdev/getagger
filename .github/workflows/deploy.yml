name: Deploy to PythonAnywhere
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy endpoint
        run: |
          curl -s -X POST "https://getagger.pythonanywhere.com/deploy/${{ secrets.DEPLOY_TOKEN }}/"
